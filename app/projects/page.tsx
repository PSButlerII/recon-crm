import { PageHeader } from "@/components/page-header";

export default function ClientsPage() {
  return (
    <PageHeader
      title="Projects"
      description="Manage the projects you are working on."
    />
  );
}